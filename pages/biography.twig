<!-- Biography -->
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-8">
      <h1>{{ node.field_title }}</h1>
      <div class="profile-details">
        <div id="job-title">{{ node.field_job_title }}</a>
        <div id="department">{{ node.field_lead_department }}</div>
      </div>

      <div id="introduction">{{ node.field_summary }}</div>

      <h2>Biography</h2>

      {{ node.field_body }}

      <h2>Previous roles</h2>

      {{ node.field_previous_roles }}

      <h2>Responsibilities</h2>

      {{ node.field_responsibilities }}

      <h2>Responsibilities</h2>

      {{ node.field_role }}

      <h2>Previous roles</h2>

      {{ node.field_previous_roles }}

    </div>
    <div class="col-xs-12 col-sm-4">

    </div>
  </div>
</div>
